<%= form_for([@strain, @contig]) do |f| %>
  <% if @contig.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contig.errors.count, "error") %> prohibited this contig from being saved:</h2>

      <ul>
      <% @contig.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :length %><br>
    <%= f.number_field :length %>
  </div>
  <div class="field">
    <%= f.label :sequence %><br>
    <%= f.text_field :sequence %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
